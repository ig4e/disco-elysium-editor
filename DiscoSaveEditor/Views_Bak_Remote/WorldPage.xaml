<Page
    x:Class="DiscoSaveEditor.Views.WorldPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DiscoSaveEditor.Views"
    xmlns:vm="using:DiscoSaveEditor.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowDefinitions="Auto,Auto,*">
        <TextBlock Text="World &amp; Variables" Style="{StaticResource HeaderTextBlockStyle}" Margin="0,0,0,24"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="20" Margin="0,0,0,16">
            <TextBox Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     PlaceholderText="Search variables..." Width="300" 
                     Header="Filter Variables"/>
            <ToggleSwitch IsOn="{x:Bind ViewModel.IsFogOfWarEnabled, Mode=TwoWay}" 
                          Header="Fog of War" OffContent="Disabled (Revealed)" OnContent="Enabled"/>
        </StackPanel>

        <ListView Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.Variables}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:VariableDisplayItem">
                    <Grid ColumnDefinitions="*,150,200" Padding="8">
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,0">
                            <TextBlock Text="{x:Bind Key}" FontWeight="Bold"/>
                            <TextBlock Text="{x:Bind Description}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="Gray" TextWrapping="Wrap"/>
                        </StackPanel>
                        
                        <TextBlock Grid.Column="1" Text="{x:Bind Type}" VerticalAlignment="Center" Foreground="LightGray"/>

                        <TextBox Grid.Column="2" Text="{x:Bind Value, Mode=TwoWay}" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
