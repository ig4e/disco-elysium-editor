<Page
    x:Class="DiscoSaveEditor.Views.ThoughtCabinetPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:DiscoSaveEditor.ViewModels">

    <Grid Padding="32,24,32,32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Thought Cabinet" Style="{StaticResource TitleTextBlockStyle}" Grid.Column="0" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="Internalize All" Command="{x:Bind ViewModel.InternalizeAllCommand}" VerticalAlignment="Center" />
            <Button Grid.Column="2" Content="Reset All" Command="{x:Bind ViewModel.ResetAllCommand}" VerticalAlignment="Center" />
        </Grid>

        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Thoughts}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:ThoughtDisplayItem">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,2">
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36" />
                                <ColumnDefinition Width="180" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- State emoji -->
                            <TextBlock Grid.Column="0" Text="{x:Bind StateEmoji, Mode=OneWay}" 
                                       FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" />

                            <!-- Name -->
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{x:Bind DisplayName}" FontWeight="SemiBold" />
                                <TextBlock Text="{x:Bind ThoughtType}" FontSize="11"
                                           Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                            </StackPanel>

                            <!-- State -->
                            <TextBlock Grid.Column="2" Text="{x:Bind State, Mode=OneWay}" 
                                       VerticalAlignment="Center" FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                            <!-- Bonuses -->
                            <StackPanel Grid.Column="3" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind BonusWhenCompleted}" FontSize="11" TextTrimming="CharacterEllipsis"
                                           Foreground="{ThemeResource SystemFillColorSuccessBrush}" MaxLines="1" />
                                <TextBlock Text="{x:Bind BonusWhileProcessing}" FontSize="11" TextTrimming="CharacterEllipsis"
                                           Foreground="{ThemeResource SystemFillColorCautionBrush}" MaxLines="1" />
                            </StackPanel>

                            <!-- Cycle state button -->
                            <Button Grid.Column="4" Content="Cycle State" VerticalAlignment="Center"
                                    Padding="8,4" FontSize="11"
                                    Tag="{x:Bind}"
                                    Click="CycleState_Click" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
